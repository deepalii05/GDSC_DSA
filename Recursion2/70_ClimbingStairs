class Solution {
public:
    int climbStairs(int n) {
        if(n==1)return 1;
        if(n==2) return 2;
       int when1= climbStairs(n-1);
       int when2= climbStairs(n-2);
        int ans=when1+when2;
        return ans;
    }
};
